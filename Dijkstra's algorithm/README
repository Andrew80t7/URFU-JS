Этот код реализует алгоритм Дейкстры для поиска кратчайших путей во взвешенном графе. В краткости:

Создается класс Graph, представляющий граф.
Граф имеет методы addNode для добавления узла и addEdge для добавления ребра с весом между узлами.
Алгоритм Дейкстры:

Функция dijkstra принимает граф и начальный узел.
Инициализируются структуры данных для отслеживания расстояний (distances), предыдущих узлов (previous), посещенных узлов (visited) и очереди (queue).
Начальный узел устанавливается с расстоянием 0.
Затем выполняется цикл, в котором на каждом шаге выбирается узел с наименьшим текущим расстоянием.
Рассчитываются новые расстояния до соседних узлов, и если они меньше текущих, обновляются и добавляются в очередь.
Пример использования:

Создается граф с узлами A, B, C, D, E и ребрами с весами.
Вызывается алгоритм Дейкстры для поиска кратчайших расстояний от заданного начального узла (в данном случае, узла "B").
Результат выводится в консоль: кратчайшие расстояния от узла "B" до каждого узла и предыдущие узлы на пути к ним.
