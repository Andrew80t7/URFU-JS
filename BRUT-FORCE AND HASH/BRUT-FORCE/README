
Этот код реализует алгоритм поиска подстроки в строке методом "перебора". Краткое описание:

Чтение данных из файла:

Считывает содержимое файла "input.txt", разделяя его на строки.
Инициализация строк:

Задает строку S из первой строки файла и строку T из второй строки файла.
Поиск подстроки:

Запускает вложенные циклы while, где внешний цикл идет по символам строки S, а внутренний — по символам строки T.
Проверяет совпадение символов между строками S и T.
Если обнаружено полное совпадение подстроки T в строке S, выводит индекс начала вхождения подстроки в строке.
Вывод результата:

Выводит в консоль найденные индексы начала вхождения подстроки T в строке S.
