
Этот код реализует дешифрование текста, зашифрованного методом Цезаря с неизвестным сдвигом

Чтение файла "decode.txt": Считывает зашифрованный текст из файла "decode.txt" и сохраняет его в переменную text.

Определение алфавита и частот встречаемости в зашифрованном тексте:

Алфавит установлен как русские буквы.
Функция getOccurrence анализирует зашифрованный текст (text) и вычисляет частоты встречаемости букв, сохраняя результат в массив occurrence.
Чтение файла "chastoty.txt": С использованием функции readArray, читает частоты букв из файла "chastoty.txt" и сохраняет их в массив baseOccurrence.

Вычисление сдвига:

Функция findShift сравнивает частоты букв в зашифрованном тексте (occurrence) с частотами в исходном тексте (baseOccurrence).
Для каждого возможного сдвига (от 0 до 32) рассчитывается сумма разностей частот букв.
Находится сдвиг, при котором эта сумма максимальна.
Выводится найденный сдвиг в консоль.
Расшифровка текста:

Функция createAnswer использует найденный сдвиг для расшифровки зашифрованного текста (text).
Зашифрованные буквы заменяются на исходные с учетом сдвига, а остальные символы остаются без изменений.
